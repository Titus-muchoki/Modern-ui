

const Button = ({styles}) =>  (

    <button type="button" className={`py-4 px-6 bg-blue-gradient font-poppins font-medium text-[18px]
    text-black ${styles} rounded-[10px]
    `}>
      Get Started
    </button>
  );

export default Button